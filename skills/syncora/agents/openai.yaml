interface:
  display_name: "Syncora"
  short_description: "Durable project memory for coding agents"
  default_prompt: "Use $syncora to set up durable project memory here, or maintain it if this project already uses Syncora."
policy:
  allow_implicit_invocation: true
