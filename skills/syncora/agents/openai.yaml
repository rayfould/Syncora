interface:
  display_name: "Syncora"
  short_description: "Route durable project context safely"
  default_prompt: "Use $syncora to set up or adopt this workspace, or for relevant project work when Syncora is already initialized."
policy:
  allow_implicit_invocation: true
