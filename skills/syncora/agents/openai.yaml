interface:
  display_name: "Syncora"
  short_description: "Durable project memory and workflow drafts"
  default_prompt: "Use $syncora to set up and maintain durable project memory, debate an idea, draft a design, or verify a completion claim."
policy:
  allow_implicit_invocation: true
