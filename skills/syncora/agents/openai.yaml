interface:
  display_name: "Syncora"
  short_description: "Durable project memory and execution WorkPacks"
  default_prompt: "Use $syncora to set up or maintain project memory, pressure-test an idea, turn an accepted direction into a validated WorkPack, or verify completed work."
policy:
  allow_implicit_invocation: true
